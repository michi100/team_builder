import HexagonIcon from "@mui/icons-material/Hexagon";

export const BoardGrid = () => {
  return (
    <div className="BoardGrid">
      <HexagonIcon style={{ fontSize: 100, transform: "rotate(90deg)" }} />
    </div>
  );
};
